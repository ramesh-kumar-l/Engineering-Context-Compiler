# 04 — Decisions

| # | Decision | Context | Alternatives | Risk | Reversibility |
|---|----------|---------|---------------|------|----------------|
| 1 | Confirmed repo is greenfield; no existing product to assess or reuse. | Master prompt assumes valuable prior work to inventory; repo inspection found only LICENSE + 1-line README. | Treat as if prior work existed (rejected — fabricates context). | Low | N/A (factual finding) |
| 2 | Adopt the user-supplied Master System Prompt as the governing protocol for all future phases (memory-first, phase-gated, stop-after-phase). | User pasted the spec and said "execute"; it's explicit and detailed. | Freelance a different process — rejected, user intent is explicit. | Low | High — user can amend the protocol anytime |
| 3 | Bootstrapped the full 8-file + `active/` memory-bank structure now (Section 8) rather than partially, since none existed to reuse. | Section 8: "if equivalent files already exist, reuse them" — none did. | Create files lazily per-phase — rejected, Phase 0 explicitly calls for memory bank initialization. | Low | High — files can be edited/split later |
| 4 | Deferred all technology/component choices (language, storage, retrieval approach, etc.) to Phase 1. | Rule 3: no implementation before authorization; Phase 0 is assessment-only. | Pre-select a stack now — rejected, no requirements gathered yet to justify it. | N/A | N/A |

| 5 | ECC itself is built in **TypeScript on Node.js (ESM)**, with `zod` for runtime schema validation. | User authorized Phase 1 implementation without specifying a language. No existing code to infer it from. | MCP's reference SDK is TypeScript-first (Phase 10); the planned VS Code extension (Phase 12) must be TypeScript regardless; `npx`-based zero-install CLI matches the CLI-first distribution priority (Section 47); one language across CLI+MCP+VS Code avoids polyglot maintenance. | Python was the main alternative (stronger tree-sitter/AST ecosystem for later repo-analysis phases) — noted as a risk to revisit if Phase 2's code-analysis needs outgrow TS tooling. | Medium-low now (little code written); cost of switching rises each phase. | `npm run typecheck && npm run lint && npm test` all pass, 0 audit vulnerabilities. |
| 6 | Kept Phase 1 strictly to types + zod schema + tooling — no CLI entry point, no retrieval/ranking logic. | Rule 5/Section 60: don't gold-plate, don't build ahead of the authorized phase. CLI is explicitly Phase 8. | Add a minimal `ecc --version` stub now — rejected, no real command exists yet to justify a bin. | Low | High |
| 7 | Phase 2 repository analysis supports **TypeScript/JavaScript only**, uses the `typescript` compiler API in **syntactic-only mode** (no `Program`/type checker), and has **no caching** — full walk + re-parse every call. No target repo was specified by the user, so tests run against a static fixture repo (`test/fixtures/sample-repo/`). | Exit criteria only requires "at least one language" and "a basic dependency graph"; Decision #5's Python-for-tree-sitter risk didn't materialize — the `typescript` package (already a dependency for `tsc`) was sufficient and avoided adding a second AST toolchain. | Multi-language via tree-sitter now — rejected, no second language is needed yet (no-gold-plating, Section 60). Full type-checked `Program` — rejected, far more expensive and cross-file semantic resolution isn't needed until a phase actually requires it (e.g. accurate re-export symbol attribution). | Low — fixture-only validation means real-repo edge cases (huge repos, unusual tsconfig path mapping, monorepo workspaces) are untested until Phase 4+ runs against a real target. | High — each piece (classifier/resolver/analyzer) is isolated and independently swappable; adding a language means adding a new resolver module, not restructuring existing ones. |

Record future significant decisions here using: Decision / Context / Evidence / Constraints
/ Alternatives / Risks / Recommendation / Confidence / Verification / Reversibility (full
form for high-stakes calls; the compact table above is fine for routine ones).
