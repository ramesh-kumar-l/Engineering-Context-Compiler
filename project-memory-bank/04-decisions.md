# 04 — Decisions

| # | Decision | Context | Alternatives | Risk | Reversibility |
|---|----------|---------|---------------|------|----------------|
| 1 | Confirmed repo is greenfield; no existing product to assess or reuse. | Master prompt assumes valuable prior work to inventory; repo inspection found only LICENSE + 1-line README. | Treat as if prior work existed (rejected — fabricates context). | Low | N/A (factual finding) |
| 2 | Adopt the user-supplied Master System Prompt as the governing protocol for all future phases (memory-first, phase-gated, stop-after-phase). | User pasted the spec and said "execute"; it's explicit and detailed. | Freelance a different process — rejected, user intent is explicit. | Low | High — user can amend the protocol anytime |
| 3 | Bootstrapped the full 8-file + `active/` memory-bank structure now (Section 8) rather than partially, since none existed to reuse. | Section 8: "if equivalent files already exist, reuse them" — none did. | Create files lazily per-phase — rejected, Phase 0 explicitly calls for memory bank initialization. | Low | High — files can be edited/split later |
| 4 | Deferred all technology/component choices (language, storage, retrieval approach, etc.) to Phase 1. | Rule 3: no implementation before authorization; Phase 0 is assessment-only. | Pre-select a stack now — rejected, no requirements gathered yet to justify it. | N/A | N/A |

| 5 | ECC itself is built in **TypeScript on Node.js (ESM)**, with `zod` for runtime schema validation. | User authorized Phase 1 implementation without specifying a language. No existing code to infer it from. | MCP's reference SDK is TypeScript-first (Phase 10); the planned VS Code extension (Phase 12) must be TypeScript regardless; `npx`-based zero-install CLI matches the CLI-first distribution priority (Section 47); one language across CLI+MCP+VS Code avoids polyglot maintenance. | Python was the main alternative (stronger tree-sitter/AST ecosystem for later repo-analysis phases) — noted as a risk to revisit if Phase 2's code-analysis needs outgrow TS tooling. | Medium-low now (little code written); cost of switching rises each phase. | `npm run typecheck && npm run lint && npm test` all pass, 0 audit vulnerabilities. |
| 6 | Kept Phase 1 strictly to types + zod schema + tooling — no CLI entry point, no retrieval/ranking logic. | Rule 5/Section 60: don't gold-plate, don't build ahead of the authorized phase. CLI is explicitly Phase 8. | Add a minimal `ecc --version` stub now — rejected, no real command exists yet to justify a bin. | Low | High |
| 7 | Phase 2 repository analysis supports **TypeScript/JavaScript only**, uses the `typescript` compiler API in **syntactic-only mode** (no `Program`/type checker), and has **no caching** — full walk + re-parse every call. No target repo was specified by the user, so tests run against a static fixture repo (`test/fixtures/sample-repo/`). | Exit criteria only requires "at least one language" and "a basic dependency graph"; Decision #5's Python-for-tree-sitter risk didn't materialize — the `typescript` package (already a dependency for `tsc`) was sufficient and avoided adding a second AST toolchain. | Multi-language via tree-sitter now — rejected, no second language is needed yet (no-gold-plating, Section 60). Full type-checked `Program` — rejected, far more expensive and cross-file semantic resolution isn't needed until a phase actually requires it (e.g. accurate re-export symbol attribution). | Low — fixture-only validation means real-repo edge cases (huge repos, unusual tsconfig path mapping, monorepo workspaces) are untested until Phase 4+ runs against a real target. | High — each piece (classifier/resolver/analyzer) is isolated and independently swappable; adding a language means adding a new resolver module, not restructuring existing ones. |

| 8 | Phase 3 task classification is **rule-based** (weighted keyword/phrase regex per `TaskType`), not model/embedding-based, with **no new dependency**. | Exit criteria only requires "reasonable accuracy on a small labeled test set" — no requirement for ML-grade classification. A model call would add latency, cost, non-determinism, and a network dependency to something that runs on every ECC invocation. | LLM-based classification (rejected — adds cost/latency/non-determinism to a task that measured 100% on the labeled set without it; revisit only if real-world requests prove far more varied than the fixture). Embedding similarity (rejected — needs a model/vector dependency for no measured accuracy gain yet). | Low — regex signals can miss genuinely novel phrasings not resembling any labeled example; mitigated by `confidence` output (ties/no-match score low) so downstream phases can flag uncertainty instead of trusting it blindly. | High — `TASK_SIGNALS` is data, not control flow; adding/adjusting a signal or swapping in a model-based classifier later doesn't change `classifyTask`'s call signature. |

| 9 | Phase 4 evidence retrieval assigns each item a **basic per-retriever relevance score** (keyword overlap for code/test, recency for git) rather than deferring `relevance` to a placeholder value. | `EvidenceItem.relevance` (Phase 1 schema) is a required 0-1 field, so retrieval can't emit valid items without some number; exit criteria only asks for a "candidate evidence set," not ranking. | Emit a constant placeholder (e.g. always 1.0) — rejected, throws away free signal already available (keyword overlap, commit recency) and would make Phase 5's before/after ranking comparison meaningless. Build full multi-signal ranking now — rejected, that's explicitly Phase 5's exit criteria; would gold-plate Phase 4. | Low — scores are heuristic and not cross-source-comparable yet (a 0.6 code score and a 0.6 git score aren't equivalent); flagged in `active-context.md` for Phase 5 to address. | High — each retriever's scoring is internal to its own module; Phase 5 can replace or wrap it without changing `retrieveEvidence`'s signature. |
| 10 | Git evidence retrieval shells out to the real `git` CLI via `execFileSync` with **array arguments (no shell string interpolation)**, and swallows all failures into an empty result rather than throwing. | Needed commit history per file (Section 19 evidence types include Git); no existing git dependency in the project. `execFileSync(cmd, argsArray)` passes args directly to the process, never through a shell, so a path containing shell metacharacters can't be interpreted as a second command (avoids OWASP command-injection). | A git library (e.g. `simple-git`) — rejected, adds a dependency for functionality the CLI already provides losslessly (no-gold-plating). Building a shell string with interpolated paths — rejected outright as a command-injection risk. Throwing when git is unavailable — rejected, git history is supplementary evidence, not a hard requirement; a non-git target repo should still get code/test evidence. | Low — worst case is missing git evidence, not incorrect evidence; tested against an isolated temp repo so behavior doesn't depend on this project's own history. | High — isolated to `gitEvidenceRetriever.ts`; swappable for a library later without changing its exported function's signature. |

| 11 | Phase 5 ranks evidence with a **static, hand-picked per-source-type authority weight** (`SOURCE_AUTHORITY_WEIGHT`) combined multiplicatively with each item's relevance, plus a small additive specificity bonus for symbol-level matches — rather than a learned/configurable weighting. | Phase 4 flagged that relevance scores aren't comparable across source types (a 0.6 code score and a 0.6 git-recency score aren't the same signal); exit criteria requires ">1 signal" and unit tests against known-good orderings. | A single learned weight (e.g. from user feedback/click data) — rejected, no feedback loop exists yet (that's Phase 16). Normalizing relevance via z-scores per source type — rejected, needs a population of items per source to normalize against, adds complexity with no measured benefit yet. Ranking by relevance alone — rejected outright, that's exactly the gap Phase 4 flagged (a highly-relevant git commit could outrank an authoritative source file). | Low — weights are a static table, easy to inspect/adjust; wrong values degrade ranking quality, not correctness (no item is dropped, only reordered). | High — `SOURCE_AUTHORITY_WEIGHT` and `computeRankScore` are exported separately from `rankEvidence`, so the table can be swapped or made configurable later without changing the ranking function's signature. |

| 12 | Phase 6 estimates token cost with a **char-count heuristic** (~4 chars/token, plus a flat per-item overhead), not a real tokenizer dependency; selection is a **greedy, rank-order fill** (skip an oversized item, keep trying smaller later ones) rather than optimal bin-packing; compression **truncates each item's `symbols` list to 8** rather than summarizing text. | Phase 5's `active-context.md` flagged that Phase 6 needed a token-counting strategy decided up front. Exit criteria requires selecting+compressing ranked evidence into a valid package within a budget, with `excluded` reasons populated. | A real tokenizer (e.g. tiktoken) — rejected, adds a heavy/model-specific dependency for a guard that only needs to be in the right ballpark (no-gold-plating). Optimal knapsack/bin-packing over token costs — rejected, meaningfully more complex for a benefit unmeasured against real evidence volumes; greedy-in-rank-order is simpler, deterministic, and still fills leftover budget with smaller items. LLM-based summarization for compression — rejected outright, adds cost/latency/non-determinism to a step that must run on every compilation. | Low — a wrong token estimate or truncated symbol list degrades context quality, not correctness; no item is silently lost, every exclusion is counted in `excluded`. | High — `estimateTokens`/`estimateItemTokens` (`tokenBudget.ts`) and `compressEvidenceItem` (`contextCompressor.ts`) are isolated, single-purpose exports; a real tokenizer or smarter compression can replace either without changing `compileContext`'s signature. |

| 13 | Phase 7 classifies trust with a **static per-source-type rule table** (`classifyTrust`), not a learned/probabilistic model; provenance is **guaranteed by reconstruction from the item's own fields** when a caller-supplied item lacks it, rather than by rejecting such items; conflicts are detected **structurally** (same subject - path/identifier - with differing `trustLevel`), not by comparing claim content. | Exit criteria: every item has provenance; FACT/DERIVED/INFERENCE/UNKNOWN never blurred; conflicting evidence surfaced, not silently resolved. All three retrievers (Phase 4) already attach `provenance` in practice, but nothing enforced it as an invariant of the compiled package, and no code assigned a `TrustLevel` to an `EvidenceItem` at all before this phase. | A learned/weighted trust score — rejected, no feedback loop exists yet (Phase 16) to train against, same reasoning as Decision #11's authority weights. Rejecting evidence items with no provenance outright — rejected, provenance is missing only for hand-built/legacy items and the item's own `source`/`path`/`identifier` already contain enough information to reconstruct a provenance pointer without fabricating new facts. Semantic/content-based conflict detection (e.g. diffing what two docs/commits actually claim) — rejected outright, no NLP/diff capability exists in ECC; would require a new dependency or model call for a phase that explicitly forbids blurring trust levels via guesswork. | Low — a wrong trust classification affects how much weight a *consumer* gives an item, not correctness of the package itself (nothing is dropped); every classification is deterministic and inspectable. Conflict detection is a strict superset check (adds a `conflicts` field) so it can only surface more, never hide, disagreement. | High — `classifyTrust`, `ensureProvenance`, and `detectConflicts` are exported separately from `attachTrust`/`compileContext`, so the rule table or conflict-grouping key can be swapped later without changing `compileContext`'s signature. |

| 14 | Phase 8's CLI uses a **hand-rolled `parseArgs`** (no yargs/commander dependency), a real **`tsc` build step to `dist/`** (previously `noEmit`-only) with `bin.ecc` pointing at the compiled `dist/cli/index.js`, and is tested by calling its exported `runCli(argv)`/`runContext(...)` functions directly rather than spawning a subprocess. | Exit criteria: `ecc context "<task>"` runs end-to-end against a real repo and prints/saves a valid package, with documented usage. Phase 1 (Decision #6) deliberately deferred both a CLI entry point and any build step to "when the CLI phase needs an executable artifact" — that phase is now. | An arg-parsing library — rejected, one command with three optional flags doesn't justify a dependency (no-gold-plating, same reasoning as Decisions #8/#11). Testing via `child_process.spawn` against the built CLI — rejected as the primary test strategy: it would require running `npm run build` before `npm test` (coupling two scripts that are independently useful) and is slower/flakier than calling the exported functions directly, which already exercises the identical code path (`cli.ts`'s `runCli` is what `index.ts`'s shebang entry calls) — a one-time manual smoke test of the actual built binary was run instead to confirm the shebang and process wiring work. Keeping `tsconfig.json` itself `noEmit: false` — rejected, would slow down `npm run typecheck`'s fast feedback loop and conflate two different concerns (type-checking vs. emitting); a separate `tsconfig.build.json` extending it keeps both fast. | Low — a wrong arg parse or output format is a UX bug, not a correctness one (the underlying package is still validated via `validateContextPackage` before being printed); the build step only adds an artifact, it doesn't change any core module's behavior. | High — `runContext` (the pipeline orchestrator) and `runCli` (argv → pipeline → output) are exported separately from the shebang entry point, so a future MCP/skill surface (Phase 9/10) can call `runContext` directly without touching CLI-specific argv/output code at all. |

| 15 | Phase 9's skill is a **pure documentation artifact** (`skills/ecc-context/SKILL.md`, frontmatter + markdown) with **no executable code and no direct call into `runContext`**, kept in a repo-root `skills/` directory (not `.claude/skills/`) for a consumer to copy/symlink into their own agent's skills directory; verified by a **doc-consistency test** rather than a behavioral one. | Exit criteria: "a skill exists that teaches an agent when/how to invoke ECC, without duplicating existing engineering-methodology skills." Nothing in that criteria requires the skill to be code — it requires an agent to learn correct usage. | A skill that shells out to `runContext`/the CLI itself (e.g. a script invoked as part of skill execution) — rejected, conflates "teaching an agent to use a tool" with "being the tool"; the CLI (Phase 8) already is the callable surface, and MCP (Phase 10) will be the programmatic one — a skill duplicating either would be a third, inconsistent way to invoke the same pipeline. Placing the skill under `.claude/skills/` in this repo — rejected, this repo *builds* ECC, it doesn't consume it; the skill's audience is agents working in *other* repositories that install this package, so it belongs alongside the source as a distributable artifact, not wired into this repo's own Claude Code session. A broad "how to do software engineering" skill — rejected outright, that is explicitly what the exit criteria forbids duplicating; the doc is scoped to CLI invocation + output interpretation only, with an explicit "when NOT to use" section pointing planning/coding/review concerns elsewhere. | Low — a stale or wrong instruction degrades an agent's usage of ECC, it can't corrupt a compiled package (the CLI's own validation, Phase 8, still runs regardless of what an agent does with the output). Mitigated by `test/skill/skillDoc.test.ts` asserting the doc's documented flags/fields match what Phase 8 actually implemented, so a future CLI change that isn't mirrored in the doc fails CI instead of silently going stale. | High — it's a single markdown file with one small test; rewording, relocating, or replacing it (e.g. once Phase 10's MCP tool exists and some guidance should mention both surfaces) touches no other module. |

| 16 | Phase 10's MCP surface uses the **official `@modelcontextprotocol/sdk`** (new runtime dependency), its **high-level `McpServer.registerTool` API** with a zod raw-shape input schema, and **stdio as the transport** (`bin.ecc-mcp` → `dist/mcp/index.js`) — mirroring Phase 8's CLI structure exactly (`tool.ts` schema+handler / `server.ts` registration / `index.ts` shebang entry), calling the same `runContext` orchestrator with no new pipeline logic. | Exit criteria: `compile_engineering_context` exposed as an MCP tool; a real MCP client can call it and get a valid package back. | Hand-rolling the JSON-RPC/MCP wire protocol — rejected outright, that's exactly what the reference SDK exists to avoid reimplementing, and doing so would risk protocol drift from real clients. The low-level `Server` class instead of `McpServer` — rejected, the high-level API already provides declarative tool registration, input validation, and `CallToolResult` framing; no advanced feature (custom request handlers, notifications) is needed yet. SSE/StreamableHTTP as the transport — rejected for now, stdio is what locally-installed MCP tools (Claude Desktop/Code-style configs) expect; HTTP is a hosted/remote-deployment concern out of scope for this exit criteria, revisit only if a remote MCP server is ever required. Reimplementing the pipeline in `src/mcp/tool.ts` instead of calling `runContext` — rejected outright, would create a third divergent invocation path alongside the CLI, the exact duplication concern Decision #15 already flagged and avoided for the skill. | Low — thin wrapper over an already-tested pipeline, no `src/core` changes; the tool handler catches every error and returns `isError: true` instead of throwing, so a bad path or internal validation failure surfaces as a normal MCP tool error rather than killing the server connection (unlike the CLI's process-exit-code model, an MCP server has no equivalent of "exit" — protocol correctness requires the connection to survive a bad call). `@modelcontextprotocol/sdk` is the official reference implementation; `npm audit` reports 0 vulnerabilities. | High — isolated to `src/mcp/`; verified with the SDK's own `Client` class over `InMemoryTransport.createLinkedPair()` (a real MCP client/server pair in-process) plus one manual smoke test spawning the actual built `dist/mcp/index.js` as a subprocess via `StdioClientTransport`, so both the protocol logic and the shipped binary/shebang are confirmed working end-to-end. |

| 17 | Phase 11's `EvaluationEngine` lives in **`src/core/evaluation/`** (pure core - no dependency on `src/cli/`; it re-composes the same five core calls `runContext` does, with a minimal inline `RepositoryRef` instead of importing `resolveRepositoryRef`), with a separate **`src/benchmark/`** thin surface (benchmark task definitions, report formatting, a runnable `npm run benchmark` entry) mirroring the CLI/MCP pattern. The "agent alone" condition is a **simulated naive-agent baseline** (keyword grep over file paths in walk order, reading whole matched files, no ranking/trust/compression) rather than a real second LLM call. Benchmark tasks target **this repo's own real codebase** (dogfooding), not only the tiny test fixture. | Exit criteria: at least one "agent alone vs. agent+ECC" comparison run, with the metrics in [[07-evaluation]] measured, not just defined. `active-context.md` had flagged since Phase 10 that no target repository had been chosen to validate against beyond fixtures. | Invoking a real Claude/LLM API as the "agent alone" condition — rejected for now: would add a network dependency, per-run cost, and non-determinism to a test suite that has been deterministic/offline through Phases 1-10; the naive keyword-grep baseline honestly represents what an agent using only generic file-search tools (no ECC) would produce, which is what Section 43's "strong agent alone" comparison point is measuring against — revisit only if a future phase explicitly needs a live agent-in-the-loop evaluation. Folding evaluation into the CLI as a new subcommand — rejected, keeps the CLI's deliberate single-command scope (Decision #14) intact; evaluation measures ECC, it isn't an agent-facing surface like the CLI/MCP/skill. Running the comparison only against `test/fixtures/sample-repo/` — rejected as the sole benchmark: too small (3 files) for a meaningful recall/irrelevance signal; used for fast deterministic unit tests instead, with the real measured run against this repository's actual `src/` tree recorded in [[07-evaluation]]. | Low — no `src/core` module besides the new `evaluation/` one changed, no new runtime dependency; the one metric that came out unfavorable for ECC in the real run (irrelevant-evidence rate, since ECC intentionally returns supplementary git/test evidence beyond each task's narrow 2-path ground truth) was reported as-is rather than adjusted, since the point of an evaluation harness is measuring honestly, not tuning ground truth to flatter the result. | High — isolated to `src/core/evaluation/` and `src/benchmark/`; `computeConditionMetrics` and `runEvaluation` are exported separately from `runBenchmark.ts`'s entry point, so a future real-agent-in-the-loop condition could be added alongside the naive baseline without changing the metric-scoring function. |

| 18 | Phase 12's VS Code extension is a **separate `vscode-extension/` npm package** (its own `package.json`/manifest, `node_modules`, CI job) that **imports `runContext`/`validateContextPackage` directly** from `../src/cli/runContext.js` / `../src/core/schema/validate.js` and bundles them with `esbuild` (`vscode` marked `external`, everything else — including `zod`/`typescript` — inlined) into one self-contained `dist/extension.js`. The one command it contributes (`ecc.compileContext`) is exercised in automated tests by aliasing the `vscode` module import to a hand-written test double (`test/vscodeMock.ts`) via `vitest.config.ts`'s `resolve.alias`, rather than a real `@vscode/test-electron` host. | Exit criteria: right-click "Compile Engineering Context" produces a preview; extension must be a thin client over CLI/core, not a reimplementation. A VS Code extension manifest (`engines.vscode`, `contributes`, `activationEvents`, a `main` VS Code can `require()`) needs fields and a CJS-compatible output the root package (ESM, CLI/MCP `bin`s) shouldn't be forced to carry. | Spawning the built `dist/cli/index.js` as a child process instead of importing `runContext` — rejected: doubles Node startup cost per invocation, re-parses JSON that a direct function call already returns typed, and duplicates the CLI's own argv/output-formatting concerns for no benefit inside a Node host that can just call the function. Publishing `ecc` as a real npm dependency of the extension instead of a relative source import — rejected for now: no npm package is published yet (Decision #5's `npx`-based distribution is still local/`bin`-only), and a relative import keeps the extension permanently in sync with core during this still-fast-moving phase; revisit if/when `ecc` is ever published standalone. A real `@vscode/test-electron` end-to-end suite — rejected as the primary test strategy (same reasoning as Decision #14's CLI subprocess-spawn rejection): it downloads and boots an actual VS Code binary, which is slow/heavy for CI and unnecessary here since the `vscode` module's entire surface used by this extension (five APIs: `commands.registerCommand`, `window.showInputBox`/`showErrorMessage`/`withProgress`/`createWebviewPanel`, `workspace.workspaceFolders`/`getConfiguration`) is small enough to fake deterministically; a manual F5 smoke test in a real VS Code window (documented in `vscode-extension/README.md`) covers what the mock can't (menu registration actually appearing, real webview rendering). Reimplementing repo-analysis/retrieval/ranking in the extension — rejected outright, same reasoning as Decisions #14/#16/#17. | Low — isolated to `vscode-extension/`; no `src/core`/`src/cli` module changed. The mock-based tests verify command wiring and the real `runContext` pipeline (run against the fixture repo, same as `test/cli/runContext.test.ts`) but not actual VS Code menu registration or webview rendering in a live host — flagged honestly in `implementation-status.md` rather than claimed as covered. | High — `compileContextCommand.ts` (vscode-dependent glue) and `preview.ts` (pure HTML rendering) are separate, independently testable modules; swapping the child-process-vs-direct-import choice, or later publishing `ecc` and switching to a real dependency, touches only `compileContextCommand.ts`'s two import lines. |

| 19 | Phase 13's GitHub integration lives **inside the root package** as a fifth thin surface (`src/github/`: `prEventContext.ts` parses the `pull_request` event payload, `githubCommentClient.ts` upserts a marker-tagged PR comment via the plain `fetch` REST API, `runPrCompile.ts` wires them to `runContext`/`validateContextPackage`, `index.ts` is the shebang entry) plus a genuinely reusable **`src/reporting/markdownReport.ts`** (pure, GitHub-independent), run by a new `.github/workflows/pr-context.yml` job on `pull_request` (`opened`/`synchronize`/`reopened`) using the repo's own ambient `GITHUB_TOKEN`. **No new runtime dependency** — GitHub's REST API is called with Node 20's built-in `fetch`, not `@octokit/rest` or `@actions/github`. | Exit criteria: a PR gets ECC-compiled context (affected components, relevant tests, risk) posted or made available automatically. Unlike Phase 12's VS Code extension, nothing here needs a special manifest/host runtime incompatible with the root ESM package, so the separate-package split (Decision #18) doesn't apply. | A separate `github-action/` npm package mirroring `vscode-extension/`'s split — rejected: that split existed only because a VS Code manifest needs `engines.vscode`/`contributes`/a CJS `main`; a GitHub Actions step is just `node dist/github/index.js` in the same ESM package, so a second package/toolchain/CI job would be pure duplication with no corresponding constraint forcing it. `@octokit/rest` or `@actions/github`/`@actions/core` — rejected: this integration needs exactly two REST calls (list PR comments, create-or-patch one) and one env-var read; Node 20's global `fetch` covers both without a dependency, matching the no-gold-plating reasoning of Decisions #8/#11/#14. `pull_request_target` (to also comment on fork PRs, which get a read-only token under plain `pull_request`) — rejected: `pull_request_target` runs workflow code with the base branch's elevated token while still checking out PR head content, the well-known "pwn request" pattern; plain `pull_request` is the safe default and the fork-PR gap is accepted and documented rather than worked around insecurely. Reimplementing pipeline or comment-formatting logic inside `runPrCompile.ts` — rejected outright, same reasoning as Decisions #14/#16/#17/#18; `runPrCompile.ts` only wires `runContext` + `renderMarkdownReport` + `upsertPrComment` together. | Low — isolated to `src/github/` + `src/reporting/`; no `src/core`/`src/cli` module changed, no new dependency. Comment upserts are idempotent (marker-based find-and-replace) so re-running the workflow on every push can't spam the thread. Known accepted gap: fork PRs won't get a comment (GITHUB_TOKEN is read-only for `pull_request` from forks) — documented rather than fixed via `pull_request_target`'s security trade-off. | High — `runPrCompile.ts`'s three collaborators (`prEventContext.ts`, `githubCommentClient.ts`, `renderMarkdownReport`) are independently swappable; `renderMarkdownReport` has zero GitHub dependency and could be reused by a future surface without touching `src/github/` at all. |

| 20 | Phase 14's Engineering Memory persists decisions/incidents/outcomes as a **file-based JSON store per target repository** (`<repoRoot>/.ecc/memory.json`, written/read synchronously via plain `node:fs`), retrieved as ordinary `EvidenceItem`s with `source: 'memory'` (reusing the trust/ranking rules Phase 5/7 already defined for that source type) rather than introducing a new package field. The write path is a new **`ecc memory --type <decision\|incident\|outcome> --summary "..."`** CLI command (`src/cli/memoryCommand.ts`); the read path is a new **`src/core/memory/`** module (`types.ts`/`memoryStore.ts`/`memoryRetriever.ts`) wired into `retrieveEvidence` (`evidenceRetriever.ts`) alongside code/test/git, so every existing surface (CLI/MCP/VS Code/GitHub) gets memory evidence for free with zero changes to any of them. | Exit criteria: architectural decisions/incidents/outcomes persist across sessions and are retrievable as evidence in later compilations. `EVIDENCE_SOURCE_TYPES` already included `'memory'` and Phase 7's `classifyTrust`/Phase 5's `SOURCE_AUTHORITY_WEIGHT` already had rules for it (`inference` trust, 0.45 authority) — built in anticipation of this phase, unused until now. | Populating the schema's existing-but-always-empty `history: HistoricalClaim[]` field instead — rejected: `HistoricalClaim`'s `source: {type, id}` is a lighter, generic shape never wired to anything, and routing memory through it would mean either leaving the pre-built `'memory'` `EvidenceSourceType`/trust/ranking rules unused or populating both fields for the same entry (redundant); the `EvidenceItem` pipeline is the more direct, already-built path (build-on-existing, Rule 4). A database (SQLite/similar) — rejected, no-gold-plating: one JSON array per repo is trivially sufficient at this scale and needs no new dependency, matching the file-based-memory-bank precedent this very protocol itself uses. An async store (matching `walkRepository`'s async fs calls) — rejected: `retrieveEvidence`/`rankEvidence`/`compileContext` are a synchronous chain today (git evidence already shells out synchronously via `execFileSync`), and making only the memory step async would force `retrieveEvidence` itself to become async, rippling into every caller (`runContext`, the Phase 11 evaluation runner) for no benefit. Exposing `memory` recording via MCP/VS Code/GitHub too — rejected for this phase: the CLI is the one write surface needed to prove the exit criteria; other surfaces can add it later without touching `src/core/memory/` (no-gold-plating, same reasoning as Decision #14 scoping the CLI to one command). | Low — isolated to a new `src/core/memory/` module plus additive changes to `evidenceRetriever.ts` (one new call), `fileClassifier.ts` (`.ecc` added to `EXCLUDED_DIRS` so the store isn't walked as a source file), and the CLI (`argv.ts`/`cli.ts`/new `memoryCommand.ts`); no existing module's behavior changes when no memory has been recorded (`loadMemoryEntries` returns `[]`, same tolerance pattern as a non-git directory). A corrupt or missing memory file degrades to "no memory evidence," never a thrown error. | High — `loadMemoryEntries`/`recordMemoryEntry`/`retrieveMemoryEvidence` are exported separately from `evidenceRetriever.ts`'s orchestration; the storage format (flat JSON file) or write surface (CLI-only today) can be swapped/extended later without changing `retrieveEvidence`'s signature or any downstream ranking/trust/compilation code. |

| 21 | Phase 15's Verification Intelligence scores task risk with a **deterministic, rule-based `assessRisk`** (task-type weight + missing-test-coverage + low-trust-primary-evidence + surfaced-conflicts + blast-radius signals, each already computed by an earlier phase) and feeds it into `planVerification`, which populates the schema's existing-but-always-empty `EngineeringContextPackage.verification: string[]` field with a risk-scaled step list, always led by an explicit `Risk: <level> (<factors>)` line. Wired into `compileContext()` (`contextCompiler.ts`) as one new call, so every surface (CLI/MCP/VS Code/GitHub) gets a real risk-scaled verification plan for free — Phase 13's markdown report already rendered `pkg.verification` under "Suggested verification" but it was always empty until now. | Exit criteria: ECC recommends a verification plan (tests/checks) scaled to task risk. No new evidence gathering needed — `primary`/`supporting`/`conflicts` already exist by the time `compileContext` assigns `pkg.verification`. | A learned/ML risk model — rejected, no feedback/outcome data exists yet to train on (that's Phase 16); matches the same rule-based precedent as Phase 3's `classifyTask` and Phase 5's `SOURCE_AUTHORITY_WEIGHT`. Adding a new `risk`/`riskLevel` field to the schema — rejected for this phase: the exit criteria is satisfied by populating the existing `verification: string[]` field (whose first entry states the risk level and why), so no schema/type change was needed; `RiskAssessment`/`RiskLevel` are still exported from `src/core/verification/types.ts` for internal reuse and possible future exposure. Silently baking risk into step wording without ever stating the level — rejected, an agent/reviewer needs to see *why* a plan is short or long, not just the steps themselves. | Low — isolated to a new `src/core/verification/` module plus one additive line in `contextCompiler.ts`; a compilation with no evidence at all still produces a valid, non-throwing low-risk plan (`assessRisk`'s task-type-only floor). | High — `assessRisk`/`planVerification` are exported separately from `compileContext`; the scoring table or step wording can change later without touching any other module's signature. |

| 22 | Phase 16's Engineering Intelligence closes Section 72's feedback loop with a **deterministic, path-keyed outcome adjustment**: `MemoryEntry` (Phase 14) gains an optional `signal?: 'positive'\|'negative'` field, meaningful only on `type: 'outcome'` entries; a new `src/core/intelligence/outcomeFeedback.ts` reduces persisted entries to a `Map<path, number>` (summed +1/-1 per `signal`, capped at +-2 per path); `evidenceRanker.ts` and `memory/memoryRetriever.ts` each take this map as a new **optional, default-empty** parameter and apply it as a small additive nudge to rank score / relevance respectively. `runContext.ts` and `evaluationRunner.ts` (the two pipeline orchestrators) load it once via `loadOutcomeAdjustments(repoRoot)` and thread it through both `retrieveEvidence` and `rankEvidence`. | Exit criteria: outcomes feed back into ranking/memory quality over time. `active-context.md` had flagged since Phase 11/13/14 that no feedback loop existed yet for the static weight tables ranking/trust/risk all use. The CLI's one write surface (Phase 14) already lets an agent record an `outcome` entry - closing the loop only needed a way to read a *signal*, not new infrastructure. | A learned/ML re-weighting model trained on outcome history - rejected, same no-gold-plating reasoning as Decisions #8/#11/#13/#21: no meaningful volume of outcome data exists yet to train anything on, and a deterministic accumulator is trivially inspectable/reversible while a model wouldn't be. Adding a new top-level `EngineeringContextPackage` schema field for "adjusted score" - rejected, the adjustment is an internal ranking/relevance input, not part of the compiled package's public contract; nothing downstream needs to see the raw number, only its effect on ordering/relevance. Making `core/memory` depend on the new `core/intelligence` module (e.g. having `memoryRetriever.ts` call `computeOutcomeAdjustments` itself) - rejected: `evidenceRanker.ts`/`memoryRetriever.ts` only accept a plain `Map<string, number>` and have zero import from `core/intelligence`, so Phase 14/5's modules stay usable standalone and the dependency points one way, matching the roadmap's own "Engineering Memory -> Engineering Intelligence" evolution order. Unbounded accumulation (no cap) - rejected outright, a path with many recorded outcomes could otherwise swing arbitrarily far and drown out relevance/authority; the +-2 cap keeps it a nudge. | Low - every new parameter is optional and defaults to an empty map, so no pre-Phase-16 caller's behavior changes; the adjustment is additive and small relative to relevance/authority (max +-0.2 relevance, +-0.1 rank score), so it reorders/reweights within a band, it never flips a fact-tier item below a low-trust one or drops anything. Verified with unit tests plus one true end-to-end test (`runContext` against an isolated temp repo) showing a recorded negative outcome measurably lowers a decision entry's relevance in the very next compilation, and a built-binary smoke test showing the same 1.0 -> 0.9 shift. | High - `computeOutcomeAdjustments`/`loadOutcomeAdjustments` are the only place outcome entries are interpreted; the cap, weights, and even the whole mechanism can change later without touching `evidenceRanker.ts`'s or `memoryRetriever.ts`'s calling contract (both already default to "no adjustment" when the map is omitted). |

Record future significant decisions here using: Decision / Context / Evidence / Constraints
/ Alternatives / Risks / Recommendation / Confidence / Verification / Reversibility (full
form for high-stakes calls; the compact table above is fine for routine ones).
